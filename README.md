# Content Writer UI

Modern UX/UI prompt builder with dark mode, responsive layout, required keywords, language/style selection, and copy/clear output.

## Setup

```bash
npm i
npm run dev
```

Open http://localhost:3000
