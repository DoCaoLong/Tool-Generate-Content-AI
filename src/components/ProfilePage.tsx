import { Mail, UserRound } from "lucide-react";
import type { UserProfile } from "@/lib/types";

export default function ProfilePage({ user }: { user: UserProfile }) {
  return <main className="min-h-0 flex-1 overflow-y-auto px-4 py-8 sm:px-8"><div className="mx-auto max-w-3xl"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-sky-600">Tài khoản</p><h1 className="mt-1 text-3xl font-semibold tracking-tight">Hồ sơ</h1><section className="mt-8 rounded-3xl border border-slate-200 bg-white p-7 shadow-sm"><div className="flex items-center gap-5"><span className="grid h-16 w-16 place-items-center rounded-full bg-emerald-300 text-xl font-bold text-slate-950">{user.name.slice(0, 1).toUpperCase()}</span><div><h2 className="text-xl font-semibold">{user.name}</h2><p className="mt-1 text-sm text-slate-500">Tài khoản Content Studio</p></div></div><div className="mt-7 grid gap-4 border-t border-slate-100 pt-6 sm:grid-cols-2"><div className="rounded-2xl bg-slate-50 p-4"><div className="flex items-center gap-2 text-xs font-semibold uppercase tracking-wide text-slate-400"><UserRound className="h-4 w-4" />Tên hiển thị</div><p className="mt-2 text-sm font-medium">{user.name}</p></div><div className="rounded-2xl bg-slate-50 p-4"><div className="flex items-center gap-2 text-xs font-semibold uppercase tracking-wide text-slate-400"><Mail className="h-4 w-4" />Email</div><p className="mt-2 truncate text-sm font-medium">{user.email}</p></div></div></section></div></main>;
}
